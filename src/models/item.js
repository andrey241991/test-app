export class Item {
  constructor(id, name, dateRange, devices, users) {
    this.id = id;
    this.name = name;
    this.dateRange = dateRange;
    this.devices = devices;
    this.users = users;
  }
}
